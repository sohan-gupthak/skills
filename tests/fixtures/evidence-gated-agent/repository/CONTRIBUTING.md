# Contributing

## API deprecation policy

Any breaking change to a `/v1` endpoint requires shipping `/v2` first and
maintaining `/v1` for a minimum 12-week deprecation window, per the
partner integration agreement with Acme Partner Co.
